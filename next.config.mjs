/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'd0c237ffeb.clvaw-cdnwnd.com',
      },
    ],
  },
};

export default nextConfig;
